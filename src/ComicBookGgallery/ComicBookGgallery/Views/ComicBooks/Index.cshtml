@model ComicBookGgallery.Models.ComicBook[]

@{ 

}

<h2> Index </h2>

<div class = "Row">
	@foreach (var comicBook in Model)
	{
		<div class="col-md-3">
			<h4> @Html.ActionLink(comicBook.DisplayText, "Detail", new { id = comicBook.Id})</h4>
		</div>
	}
	<!-- <a href="@Url.Action("Detail", ..  -->